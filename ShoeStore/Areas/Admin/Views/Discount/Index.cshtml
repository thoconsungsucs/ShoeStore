@model List<Discount>

<partial name="_Notifications" />
<div class="container">
    <div class="row py-4">
        <div class="col-6">
            <h2 class="text-primary">Discount List</h2>
        </div>

        <div class="col-6 text-end">
            <a asp-controller="Discount" asp-action="Upsert" class="btn btn-primary">
                <i class="bi bi-plus-lg"></i>
                Create New Discount
            </a>
        </div>

    </div>
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>
                    Discount Name
                </th>
                <th>
                    Discount Value
                </th>
                <th>
                    Start Date
                </th>
                <th>
                    End Date
                </th>
                <th>
                    Active
                </th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.DiscountName</td>
                    <th>@item.DiscountValue</th>
                    <th>@item.StartDate</th>
                    <th>@item.EndDate</th>
                    <th>@item.Active</th>
                    <td class="text-end w-25 p-0 align-content-center">
                        <div class="btn-group" style="min-width: 150px;">
                            <a asp-controller="Discount" asp-action="Upsert" asp-route-id="@item.DiscountId" class="btn btn-primary">
                                <i class="bi bi-pencil"></i>
                                Edit
                            </a>
                            <a asp-controller="Discount" asp-action="Delete" asp-route-id="@item.DiscountId" class="btn btn-danger">
                                <i class="bi bi-trash"></i>
                                Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>

    </table>
</div>